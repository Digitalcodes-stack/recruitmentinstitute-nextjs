'use client'

/**
 * ChatAssistant is superseded by the unified FloatingActionGroup in RequestCallWidget
 * which neatly stacks "Call Me Now" and "WhatsApp" without overlapping.
 */
export default function ChatAssistant() {
  return null
}
